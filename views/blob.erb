<%= breadcrumb_path(repo.name, commit_id, path)  %>
<%= repo_link_to repo, "History", :page => :commits, :commit => commit_id, :path => path %><br/>
<% if blob.image? %>
  <%= blob_img_tag(commit_id, path) %>
<% else %>
  <%= blob.colorize %>
<% end %>
